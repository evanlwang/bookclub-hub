// Re-export all factories for backward compatibility
export * from "../factories";

// For backward compatibility with existing test code
export { seedStandard as seedFullScenario } from "../factories/scenarios";
